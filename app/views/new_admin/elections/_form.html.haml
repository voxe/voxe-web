= f.input :name
= f.input :country_id, collection: Country.all.order_by(name: :asc)
= f.input :date
= f.button :submit

:javascript
  $("#election_date").datepicker({
    "format": "yyyy-mm-dd",
    "weekStart": 1,
    "autoclose": true
  })

  $("#election_country_id").select2({
    "maximumSelectionSize": 1
  })
