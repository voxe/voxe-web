%h3= @candidacy.election.name + " - " + t('backoffice.edit_proposition')
%hr

= simple_form_for @proposition, url: new_admin_election_candidacy_proposition_path(@election, @candidacy, @proposition.id), html: { class: 'form-horizontal' } do |f|
  = render "form", f: f
