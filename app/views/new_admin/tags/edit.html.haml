%h2 Éditing the tag : #{@tag.name}


= simple_form_for [:new_admin, @tag] do |f|
  = f.input :name
  = image_tag "#{@tag.icon_prefix}32.png", onerror: "this.style.display = 'none';"
  = f.file_field :icon
  .form-actions
    = f.button :submit, 'Edit'
