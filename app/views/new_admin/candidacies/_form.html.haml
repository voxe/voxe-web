= simple_form_for [:new_admin, @election, @candidacy], html: {class: "form-inline"}  do |f|
  = f.label :candidate_id, "Sélectionner un candidat : "
  = f.input_field :candidate_id, collection: @available_candidates.sort_by{ |c| c.name.strip  }
  = f.submit "Ajouter", class: 'btn'
