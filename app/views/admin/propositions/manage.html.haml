.row
  .span16
    %h1
      = @election.name
      %small= @candidate.name
    %br

    - for proposition in @propositions do
      = render 'proposition_fields', proposition: proposition
      %hr
    = form_tag [:admin, :propositions] do
      .add_fields
        = link_to_function("Add a proposition", "add_fields(this, 'propositions', '#{escape_javascript(render 'proposition_fields', proposition: Proposition.new(electionId: @election.id, candidateId: @candidate.id))}')")
