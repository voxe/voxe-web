.container
  .content
    .page-header
      %h1= @election.name
  
    %h2 Candidates
    - @election.candidates.in_groups_of(4) do |candidates|
      .row
        - candidates.each do |candidate|
          .span4            
            = link_to candidate.name, election_themes_path(@election, candidate.namespace)